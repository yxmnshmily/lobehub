# 管理员端页面与移动排版复查（2026-09-09）

## 目标与范围

- 以管理员功能完整为前提，不通过隐藏操作规避排版问题。
- 按公共壳层、群组主流程、资源与生成、设置与统计、长内容与可访问性五个批次排查。
- 覆盖桌面路由、移动路由、Workspace 镜像路由及受邀群身份分支。

## 已实修（本轮）

- [x] 通用 `NavHeader` 三槽增加可收缩约束，页头标题超长时截断，防止面包屑 + 标题 + 操作共同撑宽页面。
- [x] 受邀群在窄屏 / 手机端也挂载群侧栏宿主，成员、话题、档案等入口不再整体消失。
- [x] 群成员与群话题页头改用群侧栏开关，不再误显示全局左栏开关；触摸热区为 44px。
- [x] 群话题 “加载更多” 失败与首屏失败分离，保留已成功加载的话题，仅在列表尾部提供重试。
- [x] Workspace / 个人统计共用页自动感知窄屏，四列指标改为两列。
- [x] 统计热力图与用量明细增加明确错误态和重试，不再把失败冒充成永久 Loading / 空表。
- [x] 移动用量表不再 `overflow: clip` 强裁四列，改为可访问的横向滚动并保留关键列。
- [x] 资源搜索浮层在移动运行时强制使用 Masonry，查询预览数据与实际渲染使用同一有效视图，避免持久化列表模式撑出 860/1040px 宽度。
- [x] 资源 Masonry 卡片的选择、更多与快捷操作在触屏上持续可见，键盘聚焦时可见，热区统一不小于 44px。
- [x] 图片 / 视频生成结果操作不再仅依赖 hover；触屏、窄屏和键盘聚焦均可达。
- [x] 视频生成批次补齐提示词、元数据和创建时间的窄屏换行规则，批次操作在触屏可见且热区不小于 44px。
- [x] 群话题权限查询失败时显示可重试警告，不再把管理员静默伪装成只读；加载更多失败也不再撤掉已有管理操作。
- [x] 话题列表行和卡片补齐键盘可达语义、Enter 打开与可见焦点，共享覆盖普通智能体和群话题。
- [x] 手机用量表直接展示输入 / 输出 Token 明细，不再将关键数据放在不可聚焦的 hover Tooltip 中。
- [x] 320px 级窄屏资源卡改为单列，卡片按操作区实际高度预留 60px，防止操作遮挡正文。
- [x] 图片生成批次补齐 `focus-within` 和手机 44px 按钮热区；图片 / 视频删除与视频下载失败增加可见提示。
- [x] 聊天输入区 “展开” 按钮在键盘聚焦 / 父容器 `focus-within` 时可见，不再只能靠鼠标 hover 发现。

## 已定位，待后续批次修复

### P1：功能或主信息可用性

- [ ] 管理员权限首次加载会替换整个主壳，导航和内容同时闪变。
- [ ] Electron 分屏 / 多标签的侧栏 registry 只按 `navKey` 存储，相同页类型可相互覆盖。
- [ ] 中等窄度下 `workspace-settings` / `project` / `resourceLibrary` / `agent-docs` / `eval` 仍保持至少 240px 侧栏，持续挤压主内容。
- [ ] 受邀群的部分子路由仍通过 pathname 手工分发，与 owner 的 `<Outlet />` 链路不一致。
- [ ] 群侧栏外层和成员 / 话题内层共存多层滚动，在矮屏可造成滚轮陷阱。
- [ ] 资源列表 / Masonry 的主点击面仍是普通 `div`，缺少键盘打开语义。
- [ ] 聊天输入框对普通 Tab 的行为与页面焦点导航冲突，需在 “编辑缩进” 和 “可离开输入区” 之间明确产品取舍。
- [ ] 超长 AI 回复始终整段渲染，没有与用户长消息一致的折叠 / 详情策略。

### P2：响应式、密度与反馈

- [ ] Workspace `navKey` 依赖异步的 active slug，首帧可先出现 Home 侧栏再切回 Workspace。
- [ ] 开启促销 Banner 时 Web/Electron 主容器的高度扣减分支不一致，底部存在裁切风险。
- [ ] 未知 `/settings/:tab` 会静默渲染 Appearance，地址、侧栏和内容可互相矛盾。
- [ ] 群成员 / 话题新页面存在整页硬编码中文，非中文 locale 会混用语言。
- [ ] 输入历史弹层缺少 `listbox/option` 语义与当前项宣告。
- [ ] `/me/profile` 和 `/me/settings` 的返回按钮尚未统一到 44px 手机页头 token。
- [ ] 头像编辑提示仅 hover 可见，触屏可发现性不足。
- [ ] 资源上传超尺寸图片与部分上传失败仍缺少就地错误提示。
- [ ] 资源搜索清空按钮的图标热区与无障碍名称仍需统一。
- [ ] 图片 / 视频引用缩略图堆叠操作仍存在 hover 依赖和小热区。

## 验证状态

- [x] 路由树、侧栏注册链、共享页头、设置、统计、资源、生成、群组和聊天输入做了跨调用链静态复查。
- [x] Impeccable layout detector 扫描目标范围，结果为空；人工复查另行发现上述结构问题，未以检测器空结果代替布局判断。
- [x] 共享导航、群分页 / 权限、统计错误态、资源视图与话题键盘行为均有聚焦回归；纯 CSS 显隐和失败 toast 完成静态检查，仍需并入真实触屏验收。
- [x] 13 个聚焦测试文件共 64 项回归通过；目标变更文件 lint clean，`git diff --check` 通过。
- [x] 隔离浏览器的 390×844 无权错误态实测 `clientWidth=390` / `scrollWidth=390`，无水平溢出。
- [ ] 完整的 390×844 / 360×800、浅色 / 深色真实登录态浏览器矩阵仍未完成；当前 Chrome 有另一扩展 UI 占用自动化会话，隔离浏览器又无登录态，不得用单元测试冒充视觉验收。
- [ ] 资源搜索当前有效视图回归为纯逻辑测试；还需在真实登录态断言查询与 Masonry 渲染同时使用移动视图。
- [ ] 浏览器控制台另见 `localhost:9876` 的 `code-inspector-plugin` 访问 React 19 已移除 `element.ref` 的错误；与本轮页面修复分开记录。

## 第六期当前状态（2026-09-12，v2.2.17）

以下为当前增量状态；上文第一轮和第五期测试计数仍只是历史证据。

- 升级基线：`1015ea4cc6`。接收时工作区 clean；由升级任务明确释放写入范围后才修改。

- 已补齐资源 ListView 文件名与 Masonry 主点击面的键盘打开，保留选择、更多、重命名和原导航 hook。事件只在自身聚焦时响应 Enter/Space，避免劫持子控件。

- 已补齐搜索清空按钮的名称、手机 44px 热区与中英文；明确清空 / Escape 后返回搜索按钮焦点，普通失焦不抢焦点。

- 已补齐 UploadCard 超尺寸、异常和无结果反馈，保留 finally 清理；共享批量上传提示被过滤的大图和失败项，继续落地成功项。

- 图片 / 视频引用在手机上展开；粗指针有样式回退；删除按钮触屏常显、键盘聚焦可见。添加圆钮与空卡共用原生 button 节点，展开不会卸载焦点。引用图按完整比例显示。

- 关联入口已检索：图片 / 视频 PromptInput、InlineImageReference/InlineVideoFrames、末帧 UploadCard、单卡与批量 hook；资源 ListView/Masonry 及共享打开 hook。权限、模型调用和文件服务端未改。

- 本轮既有红测试先复现；补充搜索与添加按钮焦点测试也先红后绿。最终本批 `bun run check`：14 项通过、lint clean。5 条组件测试策略提示来自此前已有组件测试相对 origin/canary 的比较，本批未新建组件测试文件。

- 独立只读轻审发现样式作用域与两类焦点问题，修复后唯一一次跟进确认三项解决；审查不等于视觉验收。`git diff --check` 通过。Impeccable 机械检查为空（在焦点补修前运行），不作为视觉通过证据。

- OCR 实际尝试失败：未配置有效 LLM endpoint；使用独立只读轻审提供代码审查。

- 浏览器工具可用，真实入口仍跳到登录页。管理员登录请求已发出。资源预览、实际上传与触屏换行 / 遮挡、设置 / 统计 / 群组 / 聊天路径仍等待管理员登录后验收，不能标记全部完成。

- 设置 usage/service-operations 的旧 header 断言与当前无页头设计仍待最新要求核定，不恢复旧布局。群 pathname 分发和嵌套滚动仍是待验证风险，本批不做无证据重构。

- 类型检查边界：全仓 `bunx tsgo --noEmit` 被 `.next/dev/types/validator.ts:167` 的 TS1128 阻断。本期未改该生成文件。临时范围 tsconfig 在沿用源文件与测试声明后检查本批文件及依赖，报告 313 条范围外依赖诊断，本批修改文件无诊断；不能据此宣称全仓类型通过。相关日志已保存到项目根 `artifacts/total-control-six-resource-20260912/`。
