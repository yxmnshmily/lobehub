# 超级工作群本轮验证记录

日期：2026-09-05。基线 fe40cf7993。代码未提交、未部署。初轮未重启，后续经用户明确允许重启一次，详见下文。

## 最新续做结果（覆盖下方早期阻塞状态）

### 模板健康与长会话收口（本次继续）

- 用户截图暴露受邀成员群左侧导航长期停在骨架：路由为隐藏群主专用侧栏而未注册 `group` 导航内容，全局导航误以为内容仍在加载。已增加路由级“显式隐藏导航面板”注册状态，成员群直接释放该宽度，群主和其他路由不受影响。用例先红后绿，6 文件 24 项通过。Chrome 新建验收标签仍被当前扩展界面阻断，未将真实页面热更新标记为已验收。
- 已把管理员发布的自定义助理纳入每个客户默认群健康检查和 ready 判定。缺失、禁用或配置漂移时仅重应用已发布模板；重复或未知 `supergroup-template-*` 身份只标记人工复核，自动修复拒绝执行且不删数据。聚焦检查 2 文件 31 项通过。
- 普通文本消息分页新增 `latest` 方向：首页取最新 50 条，游标稳定向更早消息翻页，各页合并后仍按时间正序。回归用例先红后绿，相关 5 文件 70 项通过；成员面板 3 文件 26 项通过。
- Chrome 已实际读取到管理员客户页四个管理范围、客户分区和健康预案；继续导航时受当前 Chrome 扩展界面阻断，没有强制接管。因此不把默认群手机实机和双账号生命周期标记为通过。

### 用户确认统一写入并重启之后

- 独立轻审membership_refresh_review已完成：本批新增行无P0/P1/P2阻塞发现；正常前台单查询轮询约120次/小时，无新定时器资源清理责任，后端权限未变。其审查未重跑测试；主线程26项/lint为上条真实命令结果。
- 用户追问同设备手机频繁退出后，切换为只读登录诊断：仅查询测试账号的设备类别/微信标记/会话时间，不读取或输出token。QQ微信iPhone在北京时间13:53和14:12各有一条会话，均有效到9月12日；Gmail手机会话也仍有效。故至少不能解释为重启清库或正常7天到期。代码getActiveSession把所有异常统一返回null，Next proxy收到null重定向signin；这是会话读取故障与无会话混淆的明确路径，但未获取对应失败请求的无敏感诊断证据，不能断言这一次就是Redis/DB异常或Cookie缺失。此诊断阶段未改认证逻辑、未延长session TTL、未放宽安全校验。
- 用户明确回复“可以”，允许本任务统一成员页写入并仅重启LobeHub。已向UI/功能任务发送不重叠写入协调消息；未覆盖自定义话题面板，长历史分页本轮仍未改。
- 仅向核验后的LobeHub devStartupSequence PID90384发送SIGTERM，由既有supervisor自动重启。新Next PID1668监听3011、SPA PID1667监听9876；官网代理PID79548保持不变。统一平台入口HTTP302（未认证请求）、官网HTTP200。未清缓存目录/数据库/聊天、未退出账号、未调用收费AI；不能凭重启宣称412受理失败已解决。
- 补齐前台跨标签刷新：useGroupRouteAccess、WorkGroupSessions、GroupInvitations的listGroups复用30秒refetchInterval、focus刷新与gcTime0；不创建额外消息总线，不改后端授权。既有layout测试先观察缺少interval/gcTime失败，再验证成员列表撤销后不再挂载会话。既有GroupInvitations真实QueryClient计时测试夹具条件hook导致lint报错，已改为每用例选择固定hook实现，保留真实定时刷新回归。
- 聚焦6文件最终26 tests passed、lint clean；3项相对origin/canary组件测试advisory来自已有测试文件，本轮未新建测试。OCR依旧缺LLM endpoint；独立轻审已派发。真实双账号跨标签退出/重入未执行。
- 手机镜像真实看到原链接“群聊不可用”，返回官网后从制作平台进入登录页，已请求用户自行登录。Chrome可读到Gmail个人中心和已加入QQ群，但连续操作遭遇用户改变窗口状态，停止争用浏览器，不发送测试内容或改账号。完整手机群聊/按钮与话题面板仍未验收。

### 再次继续：断点与剩余真实链路

- 最终冲突范围暂停：复核过程中index.tsx在主线程未实施生产补丁、三个子任务均停止的情况下，从ImperativeModal再次变为styles.mobileTopics自定义Flexbox面板。check记录的自动格式化diff仅合并条件，面板结构已在该次check开始前变化；不能将此结构归为本线程实现。最新2文件check为lint clean、26项通过，git diff --check通过，但人工审查基线漂移，需确认唯一写入者后再验收当前面板。保留所有改动，停止该文件追加写入，不覆盖还原。
- 主线程最终复核覆盖下面C早期报告：真实入口已为“历史会话·最近话题”，测试旧定位失效；改为按历史会话入口点击。继续复跑定位测试缺少应用ModalHost，补挂真实ModalHost并等待真实弹层内容，不mock弹窗成功。最终独立vitest单文件26项通过（exit 0，4.29秒）；已有node:process/KaTeX与vitest配置警告仍在。OCR本次再次因缺少LLM endpoint无法执行；主线程已人工复核本批断点/布局及测试，未把组件测试当手机实机验收。
- C将MemberConversation的移动判断由单独serverConfig.isMobile改为移动客户端或现有useIsMobile窄视口；手机宽度展示最近话题折叠入口，动作最小44px、composer不被纵向挤压。既有hook测试文件内覆盖真实组件的窄/宽客户端3场景，先RED后GREEN，C报告2文件lint clean、26 tests passed、设计机械检测为空。未新建测试文件，未修改后端。
- A只读受理失败日志：unified-dev.log的两次HTTP412调用已进入runHostedGroupChatWithBudget，groupChat.ts:164堆栈与HEAD旧版resolveAdmission catch一致，而当前源码该行无throw。当前磁盘route引用已是自动owner预算新chunk；旧chunk亦存在，不能据磁盘断言进程内存加载状态。日志无时间戳，不能强行归到13:10操作。当前groupChat/sharedBudget聚焦33项通过；没有可复现的新源码缺陷，不盲修，不去掉配置/权限校验。已向用户请求仅重启LobeHub，本轮未获明确答复、未重启、未重新调用AI。
- B定位listGroups只在窗口聚焦刷新，另一标签踢人后当前前台无操作可能保留旧界面；计划复用30秒周期但遇到账号用量上限。主线程已核对对应生产文件尚未添加轮询，不能声称跨标签回收已修复。
- C另定位普通消息listTextMessages从最早50条按createdAt/id升序分页，sendMessage只refetch已加载页可能不显示后面新消息。尚未修复；拒绝仅缓存最后一条回执的临时方案（连续发送会丢上一回执），不为补显示无界拉历史。
- 真实页面：Chrome扩展控制不可用，但同一CUA工具的原生Chrome界面读取/导航可用。已看到真实管理员客户页四分区、宽屏客户目录/详情并排、辅助资料折叠，不等于手机视觉通过。仅只读/导航，未保存资料、调余额、改模板或执行AI；用户原群草稿未动。
- 手机镜像本轮已恢复截图，当前为登录页面，已请求用户自行登录测试账号，未输入密码/验证码。双账号退出/重入及完整手机群聊仍未验收。
- 两个子任务触发账号用量上限；未购买或消费重置额度。独立审查未完成，不声称本轮发布已就绪。

### 客户页面分区与超级群继续（本次）

- 用户要求继续并行，不重做已实现的群系统。A/B/C 分别负责模板容量、成员生命周期、前端跨群状态；主线程独占客户管理布局及浏览器。未改变已有13条产品合同，未改真实群模板、未收费、未重启。
- ServiceOperations 从一条长页面拆成客户账户、超级群助理配置、内容审核、人民币服务账本四个按需挂载的分区。客户目录与详情在容器至少840px时分栏，窄屏单列且目录限高可滚动；会话IP/时间及CNY账户辅助信息改为可展开。客户四类详情与原管理操作、校验/确认不变，Tabs复用base-ui。先红后绿验证平台工具不再堆在客户页下；聚焦2文件检查lint clean、50 tests passed。原有组件测试相对origin/canary的advisory仍在，未新增组件测试文件。
- A仅扩隔离数据库容量回归：100业务助理+原固定supervisor，101拒绝后完整模板、两群关联名单不变、两账号均无overflow助理。生产未改；最终该文件8 tests passed、lint clean。此前4文件11项包含该文件，不能累加。
- B仅扩既有leave/kick回归为三用户：留群成员不能踢人，另一成员退出或重入不影响其旧文本访问，重入者仍不能访问旧历史；真实router与隔离PGlite，单文件11 tests passed、lint clean。生产未改。
- C修复MemberConversation已在途状态轮询在切群后reject，错误更新新群状态/触发onUnavailable的遗漏。catch复用已有generation校验；deferred rejection回归先红后绿。2文件23 tests passed、lint clean；A独立轻审该guard与B三用户回归无P1/P2。
- 真实只读状态更新：本次Chrome曾显示Gmail已经加入QQ群（此前13:00加入），有未发送草稿及任务受理失败提示；本任务没有替用户接受/发送/退出。无话题可交给AI是既有自动创建结果话题设计，不能把受理失败归因为缺少话题。真实退出/重入、跨标签缓存及完整手机验收仍未执行。
- Chrome独立验收标签控制先断连后提示扩展UI阻断，不声称可见弹窗。IAB为普通QQ账号，访问管理员服务运营正常返回个人中心；未绕过权限。该临时IAB标签已关闭，用户草稿/账号未动。客户页真实桌面/手机布局尚未验收；没有以50项组件测试替代视觉验证。
- OCR尝试因缺少LLM endpoint未运行，采用作者以外的子任务聚焦审查，不声称OCR通过。
- 收尾：B独立轻审客户页本次分区/布局与真实Tabs回归无P1/P2；复查A完整模板/两账号roster快照后关闭测试覆盖P2。主线程独立复跑C跨群旧请求失败回归1项通过，`git diff --check`通过，客户布局机械检测无发现；均不替代真实手机验收。

### 个人中心合并（上轮已完成）

- 原个人资料入口及标题改为个人中心，web/mobile与desktop设置映射复用CustomerCenterPage；默认账号与安全，原credits/billing入口兼容。群邀请位于账号资料前；头像/姓名/密码改为复用原ProfileSetting，保留用户名、邮箱与原认证开关。邀请提醒链接更新为`/settings/profile`。
- 46项聚焦测试与设置映射1项通过，真实Chrome个人中心已显示一套资料表单。旧CustomerCenter中antd Skeleton导入警告未扩大修复。

### 邀请提醒显著化

- 手机会话首页与桌面会话侧栏复用 WorkGroupSessions：有待接受邀请时，群列表最上方显示语义提示色块、邮件图标、数量、“查看邀请”链接及接受后才加入说明；空列表不显示。复用现有接口，页面聚焦和 30 秒间隔更新，不发送邮件/推送，不自动接受邀请。
- 入口直达 `/settings/credits?section=account-security`；目标页把群组邀请列表移到账号资料之前。源语言、中文和英文同步。
- 先验证缺少提示的失败用例，再完成补丁；聚焦 6 文件检查 45 项测试通过。唯一 lint 错误是 CustomerCenter/index.tsx 原有 antd Skeleton 导入，本轮未扩大到旧加载组件迁移；检查器有 2 个相对 origin/canary 的已有组件测试提示。机械设计检测为空。
- Chrome 曾短暂返回页面状态，导航时控制连接仍受阻，未声称受邀账号真实提示与手机镜像验收通过。没有增加真实邀请、接受或退出操作。

- 旧账号介绍：修正 getGroupDetail 的普通群主 profile 提前返回路径；仅补空介绍或精确旧默认内容，保留自定义。相关 6 文件 80 项检查通过；真实群页面已显示旅游群主负责协作架构、任务分工、秩序与分歧协调。
- 窄屏布局：默认群采用窄屏布局/输入区，280px 桌面侧栏不再挤压聊天。浏览器 390px 实测 documentWidth=390，发送按钮 x256–300、44×44 完整可见；1280px 恢复桌面侧栏。布局批次 34 项、输入相关批次 15 项通过。
- 最近话题：默认个人超级群的手机历史弹层复用最近 10 话题，不挂载普通群的筛选、更多/删除工具；普通群及 workspace 保留原行为。4 项测试及 390px 真实弹层检查通过，B 已独立复核。
- 自动群归属计费：默认个人超级群移除手动代付设置、默认成员额度开关。服务端按群主账户预留和结算实际 Token 费用，发起成员身份仍在托管任务审计中；保留余额、单次预算、成员版本验证、调用前授权、幂等及退款。普通群原有代付能力保留。未调用真实收费模型。
- 邀请界面显示自动按群所属账号结算，不再显示废弃的代付额度或误导性未开放提示；旧默认群邀请不受代付配置快照变更阻断。邀请 UI 8 项、群侧栏 UI 5 项通过，C 独立审查；真实 QQ 群主侧已无“群代付设置”，邀请与成员管理仍在。
- 后端聚焦检查 8 文件 67 项通过，额外数据库兼容回归 19 项通过。覆盖无代付 policy、成员旧 paid 标记为 false 仍按群主余额准入，余额不足不扣成员，退出后调用前拒绝，实际 Token 计费及重复结算幂等。主线程中途同 8 文件复跑曾失败：单 @ 发布查询 expected assistant、actual []。已用 deferred gate 稳定复现：流结束早于持久化发布，测试过早查询。仅调整集成测试等待真实状态接口确认持久化 done，并断言此前不可见；生产安全过滤不改。修复后 B 同 8 文件再次 exit 0、lint clean、67 项通过，A 独立时序回归审查无 P1/P2。
- 主线程最终独立运行 `TEST_SERVER_DB=0 bunx vitest run apps/server/src/routers/lambda/__tests__/integration/aiAgent/execHostedGroupAgent.integration.test.ts --maxWorkers=1 --minWorkers=1`：exit 0、4 项通过、22.34 秒。模型为 mock、数据库为隔离测试实例，无真实扣费。输出中的越权 PRECONDITION_FAILED 属于拒绝用例；本地可选 Redis/QStash 配置警告仍存在。
- 上述测试批次存在重叠，不合计为独立总数；全仓类型检查、全部手机页面和真实收费链均不作通过声明。
- 主线程合并复查侧栏、邀请提示、手机最近话题 6 文件：lint clean、17 项通过。检查器相对 origin/canary 报 3 个组件测试新增提示；本批仅扩展工作区已有测试，提示不计作测试失败。A 独立复核群主窄屏输入 6 文件，未发现 P1/P2；发送权限、预算校验及 mentionItems 保留。

### 双账号与实机边界

- 用户已允许保留手机草稿再切换；未发送草稿保存于 tasks/mobile-qa-preserved-draft.md。已从原群返回并进入默认超级群，随后手机镜像捕获报 ScreenCaptureKit -3811，未完成补丁后的完整实机验收。
- 用户明确允许 QQ 账号邀请 Gmail 账号、少量标记验收测试的普通文字及退出/重新加入；不允许收费 AI 测试。当前仅创建一条 QQ → Gmail 待接受邀请，未发送测试消息、未退出/踢出任何真人，未删除群主记录。
- Chrome 控制连接持续报 another extension UI is open，但原生界面未看到可见弹窗，不能断言用户需要关闭某个可见窗口。用户反馈看不到后复核仍是连接阻断；没有退出用户账号、重启 Chrome 或盲点弹窗。
- 群主侧真实页面仍显示待处理 y***@gmail.com、暂无真人成员。已请求用户在 Gmail 的“账号与安全 → 群组邀请与协作”接受测试邀请；接受/退出/重新加入的真实双账号验收尚未完成，隔离数据库生命周期测试不能替代此验收。
- 本轮临时浏览器视口已恢复默认，群主侧验收标签保留供继续操作。
- OCR 本轮尝试仍因缺少 LLM endpoint 无法执行，未重试或改配置；采用作者以外子任务的聚焦交叉审查。

## 续做结果（本轮）

- 已确认并修复主聊天区永久骨架：推理参数接口正常返回 undefined，被 SWR suspense 当成未加载；查询源归一为空配置 {}，不吞接口异常。真实页面现已显示群标题、欢迎语、编辑器、工具栏、模型、发送及批准控制。
- 默认群成员侧栏不再用当前用户代替隐藏的 supervisor，实际浏览器已显示“旅游群主AI”；复用既有固定身份，没有创建第二个协调助理。侧栏简介复用 Popover，仅展示头像、名字、介绍，不开放私聊/设置入口。
- 用户明确旅游群主负责协作架构、职责分工、讨论秩序及分歧协调。已更新默认工作指令和空介绍回填；只对精确旧默认指令安全升级，自定义内容保留。升级接入 check → health plan → execute → ready 路径，不只覆盖新用户初始化。
- 成员 hosted 请求支持多个 @，兼容旧单个字段，去重保序且逐个验证群范围；AI 任务超过 4000 字提示，普通文字仍允许 8000 字。停止按钮不被提示信息遮住；名单弹窗重试与 @ 选择器统一查询键。
- 默认群删除保护覆盖管理员、deleteAll、修改默认标识/软删除标记后绕过等入口；未对真实用户执行删除。
- 聚焦检查：加载源 40 项、hosted 多 @/计费/生命周期 42 项、默认群模型 84 项、群主职责/初始化/安全升级 28 项、成员 hook+面板 26 项、侧栏 3 项通过。批次有重叠，不累加为独立总数；中英文语言文件 lint clean。
- 独立交叉审查发现并补齐缓存恢复、旧账号职责升级及介绍回填路径。OCR 本轮仍因缺少 LLM endpoint 无法运行，未声称 OCR 通过。

### 重启前阻塞及本次复验

- 成员弹窗真实接口为 500。已只读核对真实 DB 列以及虚构无匹配 ID 的模型 SQL，两者正常。分段诊断最终得到 `PARTICIPANTS:NOMTH`：运行中的服务实例上 `listParticipants` 方法不存在，新源码方法未被后端旧模块缓存加载。
- 没有通过改 import 或复制 SQL 绕过旧模块，没有擅自重启。已向用户请求仅重启本地 LobeHub，再复验名单与完整群聊流程；所有临时诊断在交付前撤除。
- 手机镜像只读查看到另一个群且有未发送草稿，没有擅自发送或离开；已询问是否允许转到默认群验收。此观察不算默认超级群手机验收通过。
- 双账号接受邀请/退出重入及实际收费模型任务仍未执行；没有宣称所有按钮、所有手机页面或生产部署已经验收。

### 用户允许重启后的实测（2026-09-05）

- 用户明确允许仅重启本地 LobeHub。向已核实的 devStartupSequence PID 79581 发送 SIGTERM，现有监督器自动恢复后端 3011 和 SPA 9876；网站入口 PID 79548 保持不变。没有清数据库、删除聊天或清理构建文件。
- 新服务 PID：Next 90406、Vite 90405；入口 `/lobehub/` 返回正常登录重定向 302。冷启动阶段暂时显示加载，随后原群页面及输入区恢复。
- 真实页面点击“群成员”后正常显示 1 位群主及 5 位助理，包含旅游群主AI；搜索“旅游群主”正确过滤，仅展开介绍，没有私聊入口。原名单接口 500 阻塞已解除。
- 新发现：当前旧账号的旅游群主介绍仍为“暂未添加介绍”。代码中的旧账号修复路径并未在此次实际登录读取中完成回填，需继续检查触发路径，不能宣称职责介绍已实际生效。
- 窄窗口成员弹窗可用；背景仍是桌面侧栏挤占聊天区，不能将此当作手机布局验收通过。未操作手机镜像里的未发送草稿。

## 已实现范围

- A：复用默认群初始化，在既有群配置内保存管理员模板；事务内同步已有默认群，新用户继承；保留独立消息与真人成员；支持最多 100 个业务助理和固定协调助理。
- B：复用邀请/成员模型，接受才加入、群主踢人、成员退出、重新加入历史边界；默认群话题和消息删除保护及跨话题重绑定保护。
- C：顶部成员入口、可搜索助理名单、介绍、真人邀请/退出/踢人界面、成员群 @ 指定助理、AI 最终回复、消息翻页、等待/失败状态。
- 主线程：桌面/手机个人会话入口、对应群最近活跃 10 话题、进入最近话题、管理员模板界面与中英文文案。
- 没有另建后台或数据表，没有批量虚构助理，没有删除旧群和聊天历史。

## 自动化证据

- 最终集成回归：`bunx vitest run --maxWorkers=2 --minWorkers=1` 指定 7 个被根配置收集的测试文件，73 项通过，耗时 31.44 秒。覆盖模板数据库集成、消息删除保护、成员可见记录、首页最近话题、成员面板、管理员保存重试、成员聊天状态与路径定位。
- 数据库包的 topic.supergroup-retention.test.ts 不属于上述根配置收集范围；由 B 的数据库包聚焦检查单独验证，不能计入这 73 项。
- 分模块检查：A 模板/初始化 27 项、hosted @/话题绑定 63 项、hosted 实际执行链集成 3 项通过；B 多轮权限/记录保护检查通过（批次有重叠，不累加总数）。
- C 最终状态补丁检查 20 项通过；Avatar nullable 修复后面板 4 项通过。
- 默认英文、en-US、zh-CN 三个语言文件 lint clean；`git diff --check` 通过。

## 审查

- 子任务交叉审查，不由作者自审替代：A 审查 C；C 审查主线程、A、B。
- 修复审查发现的终态名称不一致、成员缺少 AI 最终回复、结果话题缓存刷新、超过 50 条消息无法继续加载、话题排序被前端覆盖、错误群类型出现成员入口、退出后的会话缓存、重试重复创建、路径参数未读取等问题。
- OCR 尝试一次，因未配置 LLM endpoint 无法运行；未声称 OCR 通过，使用上述独立交叉审查。

## 真实页面与未完成项

- 现有本地服务已登录账号：首页能看到“我的超级工作群”和“最近 10 个话题”；该群当前显示无话题空态，没有为测试写入真实聊天。
- 首批曾观察到主聊天区持续骨架；本轮已按上节记录复现、修复并确认完整输入区显示，但实际收费发送未测试。
- 390×844 浏览器尺寸下仍进入桌面路由构成，侧栏挤占聊天区；只修改视口不等于 iPhone 实机移动构建验收。本轮没有宣称镜像/手机验收通过；已恢复临时视口设置。
- 尚未在两个真实测试账号中执行邀请接受、退出/踢人及重新加入，也未向真实群发送 AI 任务、收费调用或管理员全员同步。上述边界目前有自动化证据，真实端到端仍待验证。
- 全仓类型检查未通过：输出含既有服务/测试类型错误；最终重跑到 60 秒保护上限并输出诊断。发现本轮新增的表单类型、Avatar nullable 等错误已修复，但未宣称全仓类型检查通过。
- 浏览器控制台有 React 19 element.ref 兼容警告，尚未定位其与加载骨架是否相关。

本轮外部并发出现的 UserPanel 和邮件品牌文件改动不属于本任务，未覆盖或合并进本任务完成声明。
