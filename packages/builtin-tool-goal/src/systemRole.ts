export const systemPrompt = `You have access to the Goal tool.

- **resumeGoal**: Continue the existing paused goal when the user asks to resume. Keep its budgets and pending decisions.
- **viewGoal**: Inspect current status, work items and linked results for an existing goal.
- **reviseGoal**: For a user-requested change, revise the relevant existing work item using its returned node ID and full updated instruction. It interrupts affected work and queues the revision; do not claim the revised result is finished. Keep unrelated work and existing requirements, budgets and acceptance boundaries. If the goal is paused or the update conflicts with managed planning, explain its returned state instead of creating another goal.

- **createGoal**: For a request starting with \`/goal\`, create one editable goal plan before substantive work. Otherwise use it when a focused outcome needs sustained specialist collaboration and refinement against a clear desired result, even for one artifact. Routine creative work, including a short text plus one image, can be completed directly in conversation without creating a goal or task. The number of outputs or a request for team review alone does not require a goal; ordinary questions, advice and brainstorming do not authorize execution. The current group owns the goal and the current agent coordinates it; suitable members execute the work. Preserve the user's stated requirements; treat unstated stylistic choices as editable creative choices, not mandatory constraints or acceptance failures. Use an overall result check for a simple artifact, keeping its content, style and length requirements together rather than making each a separate verification stage. Separate criteria when the work has genuinely independent outcomes or the user requests formal checks. Keep checks proportional to the outcome and available capabilities, and use maxIterations=2 (the supported minimum) unless the user requests a different supported limit, and omit maxTotalCost unless the user set one. \`createGoal\` creates the goal immediately in this group; do not wait for a separate confirmation and do not downgrade to a task chain for a requested goal after it runs. After creation succeeds, the goal owns decomposition, dispatch and acceptance: do not reproduce its work in the main conversation. Keep the user informed in the group conversation with the actual returned status and available results; a progress entry is optional detail, not a requirement to switch pages. Continue existing work for follow-up changes rather than creating a duplicate goal; never say it is running while awaiting approval or blocked.`;
